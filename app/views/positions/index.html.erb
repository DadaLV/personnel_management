<div class="container m-4">
  <h1><%= t('.positions') %></h1>
  <%= link_to t('button.new_position'), new_position_path, class: "btn btn-primary mb-4" %>

  <table class="table table-hover table-striped table-bordered">
    <thead>
      <tr>
        <th scope="col"><%= t('.name') %></th>
        <th scope="col"><%= t('.salary') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @positions.each do |position| %>
        <tr>
          <td scope="row"><%= position.name %></td>
          <td><%= position.salary %></td>
          <td><%= link_to t('button.show'), position, class: "btn btn-primary" %></td>
          <td><%= button_to position_path(position), method: :delete, onclick: "return confirm('#{t('confirmations.are_you_sure')}')", class: "btn btn-danger", form: { class: "d-inline" } do %>
      <%= t('button.delete') %>
    <% end %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<div>